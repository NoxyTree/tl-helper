# Unknown-formula register

Build 24118850 · 2026-07-10 · statuses: **extractable** (present in client data, not yet decoded/parsed) / **calibration-required** (must be measured in controlled play) / **likely server-only** / **currently unknown** (no evidence either way yet).

Rule: nothing below may be silently invented in the engine. Every entry ships as `modeled` or `unsupported` until its status changes with evidence.

| # | Unknown | Status | Evidence so far | Next step |
| --- | --- | --- | --- | --- |
| 1 | Damage pipeline order (stages 1–15 in 02-damage-healing-tanking.md) | **currently unknown** | Formula rows give per-stage magnitudes but no ordering; no order table found in decoded set | Calibrate with single-variable experiments; check `TLEffectProperty` + ActionTree packages (extractable candidates) |
| 2 | Armor/defense → % mitigation curve | **modeled shape; current-level constant calibration-required** | Community-tested shape is `K/(Defense+K)`. Evidence supports `K=2500` at level 50 and `K=2750` at level 55; current-cap scaling and rounding remain unverified. | Extract any level constants from the current client, then spot-check current-cap `K` and integer boundaries |
| 3 | Hit vs evasion contest curve | **calibration-required** | `all_accuracy`/`all_evasion` totals only; Questlog community model `Δ/(Δ+1000)` captured in `questlog-contest-curve-evidence.md` | Curve fitting per 04 plan; community model usable as `modeled` only |
| 4 | Crit chance vs endurance (critical_defense) curve | **modeled; glancing constant calibration-required** | Positive Crit difference uses the common contest curve. When Endurance wins, community sources agree the result is a glance selecting minimum Base Damage; the mirrored `Δ/(Δ+1000)` chance is inferred by symmetry rather than bracket-tested. | Test the glancing denominator and glancing × Heavy interaction only |
| 5 | Heavy/double-attack contest curve | **calibration-required** | Stat totals only; same Questlog `Δ/(Δ+1000)` chance reference. The separate Heavy magnitude operation is calibrated at approximately `normal * 2.284` for a displayed `+128.4%` build in `combat-log-findings-2026-07-11.md` | Fit the chance curve separately; do not conflate proc probability with magnitude |
| 6 | Block chance/efficiency vs penetration | **mixed: client-visible magnitude; chance contest calibration-required** | Current shield item data exposes `Shield Damage Reduction 40%`, independently matching repeated community observations that a successful base shield block retains 60% damage. `kAmountFromShieldBlockChance` also exists (22 rows). Exact Block Chance vs Block Penetration resolution, modifier overrides, stage order, and rounding remain unresolved. | Use `0.60` only as a versioned client-visible base block multiplier; decode-inspect formula rows and spot-check modified shields before modeling the chance contest or pipeline order |
| 7 | Basis units (10000 = 100%) and tooltip scaling (tooltip1 = mul/100) | **display encoding verified**; live magnitude basis still calibration-required | Live tooltips for Swift Healing, Gaia Crash, and Distortion Veil match decoded rows exactly at every displayed level (`calibration-findings-2026-07-10.md`); rarity tiers are 5-level windows (Epic = global +10, Heroic = +15) | Discriminate the live multiplier/rounding with protocol groups 6–7 |
| 8 | Rounding order and integer stages | **calibration-required** | Web static math uses floor at specific points (Questlog-derived); combat rounding unobserved | Integer-boundary tests per 04 plan |
| 9 | PvE/PvP skill amplification | **extractable candidate; no global PvP scalar supported** | Official 4.0.0 notes expose per-skill monster damage amplification changes. Per-skill `_PVE`/`_Boss` variants are client-visible; current evidence does not support a separate global PvP damage scalar. | Extract and version per-skill monster amplification; retain PvP caps and any undiscovered content scalars as separate boundaries |
| 10 | Server tick rate, DoT/HoT tick alignment | **likely server-only** | Durations client-visible (`*_Duration` rows, ms); alignment not | Observation-based estimation only |
| 11 | Internal cooldowns of passive procs | **extractable (partial)** | `TLAbnormalState` carries duration/stack fields; proc ICDs not yet located | Decode remaining `TLAbnormalState_*`; inspect `TLPcDynamicStat` rows |
| 12 | Threat/aggro coefficients | **likely server-only** | Nothing client-side found | Keep modeled relative indices only |
| 13 | Buff exclusivity resolution (same `ModifyGroup`) | **extractable** | `ModifyGroup` + `PriorityInGroup` + `StackCap` decoded; `TLAbnormalContentsGroup` not yet decoded | Decode `TLAbnormalContentsGroup` (288 KB) |
| 14 | Skill→formula-row naming transform, full coverage | **extractable** | Held for all sampled skills (`WP_BO_S_X → BO_X`); 95.4% of tooltip bases resolve | Materialize mapping for all 210 sets; flag the misses |
| 15 | NPC/boss defense and species modifiers | **extractable (partial)** | `TLNpc_*` raw-preserved (33 MB, undecoded); `kAmountFromTargetHpMax` rows show target-scaled magnitudes | Decode selected boss NPC tables on demand |
| 16 | Distance sampling for `kAmountFromDistance` (127 rows) | **calibration-required** | Formula rows visible; in-game sampling unknown | Controlled range tests |
| 17 | Combat-power formula | **extractable** | `TLItemCombatPower` decoded (132 rows) — not yet compared to the web's fitted heuristic | Compare; replace heuristic with labeled extraction |
| 18 | 175 unresolved tooltip bases (`KN_`, `MO_`, `SC_`, `AS_` kits) | **extractable** | Absent from `TLFormulaParameterNew`; likely NPC kit tables | Locate in undecoded families; record per-base resolution |
| 19 | Legacy `TLFormulaParameter` (non-New) relationship | **currently unknown** | Old table exists in extraction; not decoded | Decode + diff against New before trusting either |
| 20 | Whether tooltip values ever diverge from live behavior | **calibration-required** | Known MMO failure mode; no local evidence either way | Include tooltip-vs-observed checks in every calibration case |
| 21 | `HealEffect` operation, stacking, and stage order | **extractable** | Both Swift Healing magnitude rows expose the hook; the community healing workbook assumes a simple outgoing multiplier | Search decoded dynamic-stat and abnormal-state metadata before calibration |
| 22 | Healing Received applicability to self-healing | **calibration-required** | Community workbook applies it universally; the 4.2% vs 9.75% batches neither confirm nor contradict the predicted 5.33% shift | Prefer a materially larger controlled stat change or a natural external-heal comparison |
| 23 | Skill Damage Boost applicability to healing and shielding | **calibration-required** | Community workbook applies `SDB/(SDB+3000)` without resistance; Questlog uses a boost-vs-resistance contest with constant 1000 | Locate effect-type gates in client metadata, then isolate live behavior |
| 24 | Cooldown Speed operation and rounding | **calibration-required** | Community workbook models `baseCooldown/(1+speed)`; base cooldown and stat totals are client-visible | Check one visible skill pair, then probe a decimal rounding boundary |
| 25 | Buff Duration operation, exceptions, and rounding | **calibration-required** | Community workbook models `baseDuration*(1+duration)`; effect base durations and stat totals are client-visible | Test one simple buff, then refresh and recipient interactions |
| 26 | SDB vs SDR damage operation | **modeled, community-tested; current-level denominator unresolved** | Best evidence supports signed net difference first: positive `1+Δ/(Δ+K)`, negative `1-|Δ|/(|Δ|+K)`. The negative branch refutes higher-stat-only and separate-conversion models. `K=1000` is historically well-supported but may scale or have changed. | One current-cap spot-check; do not repeat three-model discrimination |
| 27 | Critical/Heavy damage-resistance floors | **client-visible floors; additive interaction modeled** | Datamined strings establish Critical cannot fall below base Critical damage and Heavy cannot fall below 150% Base Damage. Point-for-point subtraction is strongly supported but not itself an extracted operation. | Re-read the existing Heavy stat panel; one resistance-swap sanity check only if needed |
| 28 | DoT outcome and modifier rules | **mixed** | Average Base Damage, Heavy eligibility, Defense and SDB application, and exclusion of flat Bonus Damage/Damage Reduction are supported. Published evidence directly conflicts on whether DoT ticks receive Critical Damage. | Mine existing tick logs first; test only DoT crit behavior if logs cannot distinguish it |
